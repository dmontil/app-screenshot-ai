# AI Image Direct Prompt v12

Explicit left-to-right panorama set.

Final generic direction:
- Treat the 5 images as vertical crops of one wide campaign world.
- Each prompt receives its panorama position: screen 1 far-left, final screen far-right.
- Background continuity should be stronger than object variety.
- Mockups can vary angle/rotation/scale for vitality, but the full phone must remain visible and UI readable.

Cover contract:
- Screen 1 introduces the panorama, typography, app-specific hero, and product proof.
- Must include strong product proof: real/stylized device with app UI/screenshot visible.
- Buddy/hero must be app-specific, not copied from reference mascot identity.
- Realistic App Store badge allowed if crisp and official-looking.
- No cheap fake text/cards/stats/claims unless sourced from input/screenshot.

Feature contract:
- Screens 2-5 are product-proof panels in the same panorama.
- Use same sky/field/abstract base, lighting, depth, decorative vocabulary, and typography treatment.
- Phone/device full frame visible.
- Subtle different angle/position per screen.
- Hard moat: no decoration overlaps or touches phone/UI.
- No readable microcopy/numbers outside screenshot.
- Prefer background continuity over extra props.

Iteration rule:
- v12 uses `reference_01` because v11 used `reference_02`.
- Test with `count=5`.

Success criteria:
- The five screens look like one designed set.
- Background feels like a sliced horizontal panorama.
- Cover has hook + real product proof.
- Feature mockups have vitality without losing readability.
